(require 'git-commit)

;; Not sure if another package needs tobe installed
;; Symbol's function definition is void: magit-define-popup-keys-deferred
; (require 'magit-gh-pulls)
; (add-hook 'magit-mode-hook 'turn-on-magit-gh-pulls)
